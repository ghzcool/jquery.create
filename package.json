{
  "version": "1.0.6",
  "name": "jquery-create",
  "description": "Create HTML element using jQuery",
  "repository": { 
    "type": "git",
    "url": "https://github.com/ghzcool/jquery.create.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=6.10"
  },
  "dependencies": {
    "jquery": "3.2.1"
  },
  "devDependencies": {
    "babel-cli": "6.9.0",
    "babel-preset-env": "1.6.1"
  },
  "scripts": {
	"start" : "node index.js",
    "build": "babel src --out-file dist/jquery.create.js & babel src --out-file dist/jquery.create.min.js --minified"
  }
}
