{
  "version": "1.0.0",
  "name": "jquery-create",
  "engines": {
    "node": ">=6.10"
  },
  "description": "Create HTML element using jQuery",
  "dependencies": {
    "jquery": "3.2.1"
  },
  "devDependencies": {
    "babel-cli": "6.9.0",
    "babel-preset-es2015": "6.9.0"
  },
  "scripts": {
	"start" : "node index.js",
    "build": "babel src --out-file dist/jquery.create.js & babel src --out-file dist/jquery.create.min.js --minified"
  }
}
